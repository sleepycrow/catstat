[project]
name = "sync-script"
version = "0.1.0"
description = "Syncs files to backend"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "watchdog>=6.0.0",
]
